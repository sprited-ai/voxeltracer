diff --git a/node_modules/gl-react/gl-react.js b/node_modules/gl-react/gl-react.js
index a54e361..cd2f4d6 100644
--- a/node_modules/gl-react/gl-react.js
+++ b/node_modules/gl-react/gl-react.js
@@ -4795,6 +4795,7 @@ module.exports = [
   , 'sqrt'
   , 'step'
   , 'tan'
+  , 'texture'
   , 'texture2D'
   , 'texture2DLod'
   , 'texture2DProj'
diff --git a/node_modules/gl-react/lib/Node.js b/node_modules/gl-react/lib/Node.js
index 7bdabe7..f542254 100644
--- a/node_modules/gl-react/lib/Node.js
+++ b/node_modules/gl-react/lib/Node.js
@@ -143,12 +143,16 @@ const parseInterpolation = (gl, i) => {
 
 // minimal version of gl-fbo
 const createFBO = (gl, width, height) => {
+  if (!gl.getExtension('EXT_color_buffer_half_float')) {
+    throw new Error("EXT_color_buffer_half_float not supported.");
+  } 
+
   var handle = gl.createFramebuffer();
   gl.bindFramebuffer(gl.FRAMEBUFFER, handle);
   var color = gl.createTexture();
   if (!color) throw new Error("createTexture returned null");
   gl.bindTexture(gl.TEXTURE_2D, color);
-  gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, width, height, 0, gl.RGBA, gl.UNSIGNED_BYTE, null);
+  gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA16F, width, height, 0, gl.RGBA, gl.HALF_FLOAT, null);
   gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
   gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
   gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.NEAREST);
@@ -166,7 +170,7 @@ const createFBO = (gl, width, height) => {
         width = w;
         height = h;
         gl.bindTexture(gl.TEXTURE_2D, color);
-        gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, w, h, 0, gl.RGBA, gl.UNSIGNED_BYTE, null);
+        gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA16F, w, h, 0, gl.RGBA, gl.HALF_FLOAT, null);
       }
     },
     dispose: () => {
@@ -459,7 +463,7 @@ class Node extends _react.Component {
 
     this._bind();
 
-    gl.readPixels(x, y, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
+    gl.readPixels(x, y, w, h, gl.RGBA, gl.HALF_FLOAT, pixels);
     return (0, _ndarray.default)(pixels, [h, w, 4]).step(-1, 1, 1).transpose(1, 0, 2);
   }
   /**
diff --git a/node_modules/gl-react/src/Node.js b/node_modules/gl-react/src/Node.js
index 6ca0c78..6f94228 100644
--- a/node_modules/gl-react/src/Node.js
+++ b/node_modules/gl-react/src/Node.js
@@ -266,6 +266,10 @@ const createFBO = (
   width: number,
   height: number
 ): Framebuffer => {
+  if (!gl.getExtension('EXT_color_buffer_half_float')) {
+    throw new Error("EXT_color_buffer_half_float not supported.");
+  } 
+
   var handle = gl.createFramebuffer();
   gl.bindFramebuffer(gl.FRAMEBUFFER, handle);
   var color = gl.createTexture();
@@ -274,12 +278,12 @@ const createFBO = (
   gl.texImage2D(
     gl.TEXTURE_2D,
     0,
-    gl.RGBA,
+    gl.RGBA16F,
     width,
     height,
     0,
     gl.RGBA,
-    gl.UNSIGNED_BYTE,
+    gl.HALF_FLOAT,
     null
   );
   gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
@@ -308,12 +312,12 @@ const createFBO = (
         gl.texImage2D(
           gl.TEXTURE_2D,
           0,
-          gl.RGBA,
+          gl.RGBA16F,
           w,
           h,
           0,
           gl.RGBA,
-          gl.UNSIGNED_BYTE,
+          gl.HALF_FLOAT,
           null
         );
       }
@@ -608,7 +612,7 @@ export default class Node extends Component<Props, *> {
     const size = w * h * 4;
     const pixels: Uint8Array = this._captureAlloc(size);
     this._bind();
-    gl.readPixels(x, y, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
+    gl.readPixels(x, y, w, h, gl.RGBA, gl.HALF_FLOAT, pixels);
     return ndarray(pixels, [h, w, 4]).step(-1, 1, 1).transpose(1, 0, 2);
   }
 
